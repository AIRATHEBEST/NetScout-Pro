services:
  # ─── FastAPI Backend ───────────────────────────────────────
  - type: web
    name: netscout-pro-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false   # Set manually in Render dashboard
      - key: SUPABASE_URL
        value: https://jqteahtqwffjenserypk.supabase.co
      - key: SUPABASE_SERVICE_KEY
        sync: false   # Set manually in Render dashboard
      - key: REDIS_URL
        sync: false   # Set manually (use Render Redis or Upstash)
      - key: AGENT_TOKEN
        generateValue: true
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: ALLOWED_ORIGINS
        value: "*"

  # ─── React Frontend ────────────────────────────────────────
  - type: web
    name: netscout-pro-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # ─── Redis ────────────────────────────────────────────────
  - type: redis
    name: netscout-pro-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
